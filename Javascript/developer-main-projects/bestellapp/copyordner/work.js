// pay_div braucht eine eigene function 
// error
    // error bestellapp
    // geld mit 4 zahlen immer
    // start text ausblenden
//////////////////////////////////
// js
// Notizen
                // button + 
                    // button auf den ganzen div
                    // function die nur i + 1 mit der alten times function rechnet, auch in addOrder wenn ich durch den onclick ein mal hinzu füge
                    // main function RREchung weiterleiten lassen 
                    // neuen wert einbauen verbinden und einbauen
                    // anzeigen lassen
            // -
                // function minusRechen 
                // function RREchung weiterleiten wie bei plus
            // Trash function DeleteBucket()
                // connenct trash  
            // new page phone function Cart()
                // overlay-übergang
                // mobile screen
                // button auf @media basieren, davor "none"
                // javascript text bauen komplette Seite
            // rechnung zwischensumme
                // Anzahl * Price
                // copy in zwischensumme
                // + add lieferkosten 
                // lieferkosten + Gesamt
            // copy all in cart html

// wissen call
    

    // Extra:
        // overlay mit dem Zubereitungstext - mehr button
            // Paradies Burger: Tauche ein in den Geschmack des Himmels mit unserem Paradies-Burger – zart gegrilltes Premium-Patty, geschmolzener Käse wie Sonnenlicht auf der Zunge und frische Zutaten, die dich mit jedem Biss ein Stück weiter aus der Realität entführen. Ein Burger, so himmlisch, dass er den Alltag vergessen lässt.


//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

// wie baue ich mit einer if CartStyle &&  zusammen
// addOrder && addTimes verbinden in eine function

